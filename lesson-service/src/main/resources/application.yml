server:
  port: 8082

spring:
  application:
    name: lesson-service
  datasource:
    url: jdbc:postgresql://localhost:5432/lessondb
    username: tutor
    password: tutor123
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: tutor
    password: tutor123
    connection-timeout: 10000
    template:
      retry:
        enabled: true
        initial-interval: 1000
        max-interval: 10000
        multiplier: 2
    listener:
      simple:
        retry:
          enabled: true
          max-attempts: 3
          initial-interval: 1000

eureka:
  client:
    service-url:
      defaultZone: http://admin:admin123@localhost:8761/eureka/
  instance:
    prefer-ip-address: true

video:
  zoom:
    account-id: lSuAdT2kQ0eGi7Ew1cDX0w
    client-id: peTq3mQqRkG7YCeHGM5LfA
    client-secret: NmBuwRQmKvGXzsGOx8266sX0R9lTG1YR
    base-url: https://api.zoom.us/v2
    oauth-url: https://zoom.us/oauth/token

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

logging:
  level:
    ru.tutorplatform.lesson: DEBUG
    org.springframework.amqp: DEBUG